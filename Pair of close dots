# 정렬된 1차원의 점들이 주어졌을 때, 그 중 가장 거리가 짧은 것의 쌍을 출력하는 함수를 작성하시오.
# (예를 들어 s = {1, 3, 4, 8, 13, 17, 20}이 주어졌다면, 결과값은 (3, 4)가 될 것이다.)
import random

def getPair(s):
    close = 20
    res = 0
    for k in range(len(s)-1):
        temp = abs(s[k]-s[k+1])
        if temp < close:
            close = temp
            res = (s[k], s[k+1])

    return res


def getRandoms():
    return random.sample(range(20), 10)

s = sorted(getRandoms())

print(s, getPair(s), sep='\n')
